# hello-world.yaml
name: Hello World!

on:
  push:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - run: echo "🎉 The markus-job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This markus-job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - run: echo "XXXXXXXXX"
      - run: echo "owner= ${{ context.repo.owner }}"
      
